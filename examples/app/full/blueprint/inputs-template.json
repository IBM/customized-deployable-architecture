{
  "name": "${BlueprintName}",
  "tags": ["${Prefix}-test"],
  "source": {
    "source_type": "git_hub",
    "git": {
      "git_repo_url": "https://github.com/IBM/customized-deployable-architecture",
      "git_repo_folder": "/examples/app/full/blueprint/full.yaml",
      "git_branch": "main"
    }
  },
  "inputs": [
    {
      "name": "ibmcloud_api_key",
      "value": "${APIKEY}"
    },
    {
      "name": "prefix",
      "value": "${Prefix}"
    },
    {
      "name": "ssh_key",
      "value": "${SSHKEY}"
    }
  ],
  "description": "TEST: Apache Server with Custom Infrastructure",
  "resource_group": "${ResourceGroup}",
  "location": "${Location}"
}
