- name: Hello world ansible post playbook
  hosts: localhost
  tasks:
    - name: Post Validate Environment
      debug:
        msg:
          - "Hello From POST validate, Environment is:"
          - "{{ansible_env}}"